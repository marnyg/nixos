#!/usr/bin/env bash
# shellcheck shell=bash

# Source local overrides if they exist
source_env_if_exists .envrc.local

# For now, use no-pure-eval for simplicity
# TODO: Implement pure eval with dynamic root when devenv supports it better
use flake . --no-pure-eval --fallback
